#include <cstdio>

int n, m;
int main()
{
	int T; scanf("%d", &T);
	for (; T; --T)
	{
		scanf("%d%d", &n, &m);
		int Q; scanf("%d", &Q);
		for (; Q; --Q)
		{
			char op[10];
			scanf("%s", op);
			if (op[0] == '
		}
	}
	return 0;
}
